interface NavbarProps {
}

const QueryBody: React.FC<NavbarProps> = ({ 
}) => {
  return (
    <div>
        <textarea>
            Input here
        </textarea>
        <button>
            
        </button>
    </div>
  );
};

export default QueryBody;